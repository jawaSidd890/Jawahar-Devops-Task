pod is the smallest unit of kubernetes

kubectl apply -f pod.yaml

apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app   -
spec:
  containers:
    - name: my-container
      image: nginx:latest
      ports:
        - containerPort: 80
